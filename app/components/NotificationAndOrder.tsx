import React from "react";
import { View, Text } from "react-native";

const NotificationAndOrder = () => {
  return (
    <View>
      <Text>Notification and Order Screen</Text>
    </View>
  );
};

export default NotificationAndOrder; // ✅ Ensure default export
